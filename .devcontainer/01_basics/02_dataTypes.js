"use strict" //means convert all JS code which is written in older version to newer version

//alert(3+3) it will give error in JS but if we run it on browser console it will execute

//console.log(3+3) console.log(3+3) it will give error.If we separate two console by comma it will not give error second move one console to next line

//datatypes:- number, string boolean, (null:-standalone value), undefined, symbol=>for unique

let age=18
let name="ankit"

console.log(typeof "ankit");
console.log(typeof age)
console.log(typeof null) // null type is object
console.log(typeof undefined)

